# PANTERA D2 连续工作模式

本文件是本仓库的持久工作上下文，也是修改与优化日志。进入本项目的新对话必须先阅读本文件，再检查当前工作区差异；完成修改后，必须把最新记录写在修改日志标题下方、旧记录之前。

## 1. 工作规则

1. 主要算例目录为 `D2/`，主输入为 `D2/input`，主反应文件为 `D2/deuterium_modified.react`。
2. 工作区长期存在未提交修改。不得使用 `git reset --hard`、`git checkout --` 或其他会覆盖用户改动的操作。
3. 修改前先确认现状，修改后至少运行与风险相称的检查；涉及核心 Fortran 代码时运行 `make test`，必要时再运行构建或短时算例。
4. 不得改变恒流模式的进入条件：仍然要先等待实际电流达到目标值，再切换到恒流控制。
5. 恒流 PID 必须依据“目标电流减测量电流”的误差控制，不能只依据平均窗口内的上升或下降趋势。
6. 反射粒子不得按吸收粒子计入阴极电流或介质表面电荷；SEE 必须按入射、反射/转化和逸出电子的净电荷统一记账。
7. 反应截面不得超过来源数据的最大能量做外推；超出表格范围时截面应为零或采用代码中明确的非外推处理。
8. 对经验参数、截面或 SEE 做调整时，在日志中记录旧值、新值、依据、影响范围和验证结果。引用文献时保留 DOI 或直接链接。
9. 不把推测写成已验证结论。日志状态使用：`已实现`、`已验证`、`待验证`、`已回退`。
10. 除非用户明确要求，不修改与当前任务无关的 H2 算例、融合后处理和已有未提交文件。

## 2. 新对话接手流程

1. 阅读本文件的“当前基线”和最近三条日志。
2. 运行 `git status --short`，确认用户未提交改动范围。
3. 查看本次任务涉及文件的局部差异，不以 Git HEAD 直接覆盖当前参数。
4. 向用户简要说明将沿用的关键约束，尤其是恒流切换逻辑、截面禁止外推和玻璃壁面模型。
5. 实施修改并验证。
6. 在“修改日志”顶部追加新条目；同一天可使用时间或序号区分。

## 3. 当前基线（2026-07-31）

### 3.1 物理与反应

- 已修复 e-D2 弹性碰撞把约 4/7 平动能错误转入 D2 内能的问题。
- 已处理振动激发截面过高和碰撞后重复扣除激发能的问题。
- 当前采用非解离离子碰撞电离通道：`D+ + D2 -> 2D+ + e-`。
- 反应截面能量上限应保持在各自文献/数据文件的最大能量以内，禁止继续外推。
- 用户此前要求禁用不需要的反应产物；后续重新启用任何产物前必须再次确认。

### 3.2 恒流控制与电流记账
- D2 阴极启动时先在 `100 ns` 内以三次 smoothstep 从 `0 V` 爬升到配置的恒压值；爬升电流不参与恒流切换判定。
- 爬升结束后清空电流窗口，在完整恒压测量窗口中达到目标电流后才进入恒流模式。
- 保留原定模式切换流程：电流达到目标值后才进入恒流模式。
- PID 调节方向必须由目标误差决定；电流偏大时不得继续把阴极电压绝对值调高。
- 反射粒子不计作吸收电流。
- 核心边界记账测试位于 `tests/test_boundary_control_core.f90`。

### 3.3 当前玻璃壁面

- `wall` 是实际装置的 Duran/硼硅玻璃壁面，场边界保持 `dielectric`。
- 当前网格只有玻璃表面标签，没有玻璃实体体积区。因此当前模型是表面片电荷近似，不能直接设置真实玻璃厚度或体积相对介电常数。
- 若未来网格加入玻璃实体区，可使用 Duran 室温相对介电常数约 `4.6`；在此之前不要用人为的快速表面电荷泄放替代真实玻璃。
- `D2/input` 中玻璃壁使用材料 ID 10（Duran），SEE 已开启。
- 当前启用 `D2/wall.wall`：玻璃壁离子按种类和能量保留约 `30%--55%` 的中性化回收，中性 D/D2 保留约 `21%--33%`；各能段反应概率和为 1。
- 玻璃壁电子仍采用受限分段模型：`E < 80 eV` 为 100% 吸收；高能段 90% 吸收，最多 10% 主电子由 `energy_cll` 反射；SEE 另行计算。
- `D2/wall2.wall` 保留为全吸收对照模型，除电子外所有粒子 100% 吸收，但当前未启用。
- 当前 SEE 参数：
  - SUS304 电极：电子 `delta_max=1.50`，离子 `gamma_max=0.42`，快中性粒子 `gamma_max=0.045`；
  - Duran 玻璃：电子 `delta_max=1.30`，离子 `gamma_max=0.18`，快中性粒子 `gamma_max=0.022`。
- 当前高能主电子最大反射率：阳极 `30%`、阴极 `40%`、玻璃 `10%`。
- 真实二次电子产额与包含背散射电子的总产额不能重复叠加。

### 3.4 已知限制与下一步诊断

- 玻璃实体、电介质厚度及外侧接地结构尚未进入网格；当前模型无法完整描述玻璃电容和外侧位移电流。
- 判断异常鞘层时，应同时检查局部表面电荷、壁面电势、离子/电子入射通量、SEE 产额和高能主电子反射，不能只看全局粒子数。
- SEE 开启后的首轮算例应与关闭 SEE 的同工况对照，并检查局部量而非只比较总电流。

## 4. 验证命令

```bash
make -j2
make test
```

当前 `make test` 包含边界净电荷/PID、相对论碰撞核心和 P0 静态检查。

## 5. 文献与数据依据

- Yoon et al., electron collision cross sections for molecular hydrogen/deuterium: <https://doi.org/10.1063/1.2838023>
- SCHOTT DURAN technical data（室温、1 MHz 下介电常数约 4.6）：<https://media.schott.com/api/public/content/b5e2b351affc45259c0b3f41d48abb78?v=e6464fab>
### 2026-07-31 — 新增独立电压爬升并修复材料 SEE 峰值

- 状态：`已实现`、`已验证`；完整放电维持效果 `待验证`。
- 用户目标：避免初始高压瞬时建立造成种子等离子体快速排空，同时修复材料文件中被过度放大的 SEE 参数。
- 修改文件：`src/global.f90`、`src/boundary_control_core.f90`、`src/fields.f90`、`src/initialization.f90`、`D2/input`、`D2/see_materials.dat`、`tests/test_boundary_control_core.f90`、`tests/test_static_p0.py`、`AGENTS.md`。
- 电压爬升：新增 `Boundary_voltage_ramp` 输入；D2 阴极在 `100 ns` 内按三次 smoothstep 从 `0 V` 爬升至当前恒压启动值 `-30 kV`，端点斜率为零。
- 控制器行为：爬升阶段禁止进入恒流模式并持续清空瞬态电流窗口；到达 `-30 kV` 后重新收集完整窗口，仍只在实际平滑电流达到 `10 mA` 目标后切换恒流 PID。PID 的目标误差方向未改。
- SEE 参数变化：SUS304 离子动能分支峰值 `gamma_max: 2.90 -> 0.42`；Duran 电子 true-SEE 峰值 `delta_max: 3.80 -> 1.30`。SUS304 电子/快中性峰值保持 `1.50/0.045`，Duran 离子/快中性峰值保持 `0.18/0.022`。
- 物理或文献依据：材料文件的峰值参数是 true secondary 上限，不能把为维持放电而放大的经验峰值与壁面文件中的主电子反射再次叠加；参见 NASA 对 true secondary 与 total yield 的区分：<https://ntrs.nasa.gov/api/citations/19820006353/downloads/19820006353.pdf>。不锈钢离子诱导发射对表面氧覆盖高度敏感，单一高峰值不能视为通用材料常数：<https://www.nist.gov/publications/low-energy-ion-induced-electron-and-ion-emission-stainless-steel-effect-oxygen-coverage>。
- 玻璃壁兼容性：保留用户当前启用的 `wall.wall` 粒子回收模型，未恢复 `wall2.wall` 的非电子粒子全吸收；静态测试改为验证当前各能段概率归一化和玻璃高能主电子最多 10% 反射。
- 验证命令与结果：`make -j2` 完整构建通过；`make test` 全部通过，包括边界净电荷/爬升/PID、相对论碰撞核心和 10 项静态检查。
- 隔离启动验证：在 `/tmp` 单进程短时算例中成功读取爬升配置；`t=0` 的 `V_cathode=0 V`，`t=1 ns` 为 `-8.94 V`，与 `100 ns` smoothstep 解析值一致。单元测试同时验证起点、中点、终点和超出持续时间后的钳位。
- 风险、限制和后续建议：本次消除了初始电场阶跃和明显失真的 SEE 峰值，但不保证单靠这两项即可达到自持；必须用新完整算例比较前 `0.2 us` 的电子损失、电离源、分边界离子通量与 SEE 产额。正在运行或旧结果不会自动采用新参数。

- NASA secondary-electron-emission report（区分 true secondary yield 与包含背散射的 total yield）：<https://ntrs.nasa.gov/api/citations/19820006353/downloads/19820006353.pdf>

## 6. 修改日志

### 2026-07-31 — 新增批量反应截面学术绘图脚本

- 状态：已实现、已验证
- 用户目标：提供一个跨平台、可直接运行的 Python 程序，批量读取不同网格的等离子体碰撞截面并生成论文级分类图和总览图。
- 修改文件：plot_cross_sections.py。
- 修改内容：新增 TXT/DAT/CSV 自动扫描，逗号/Tab/连续空格解析，表头与注释容错，首两列数值列提取，非有限数据清理，粒子类别识别，MathText 反应标签、阈值线、自动对数范围、分类图、总览图、PDF/SVG/600-dpi PNG 输出及 CSV 汇总；不对不同反应进行插值。
- 参数变化：无物理模型参数变化；所有绘图参数、标签、阈值和手动坐标范围集中在脚本开头。
- 物理或文献依据：遵循论文线图优先矢量输出、SI 单位、稀疏 marker、线型与 marker 联合编码及最终尺寸可读性要求。
- 未改变的关键行为：未修改 D2/H2 算例、反应截面数值、SEE、PID、恒流切换、壁面模型或正在运行的计算。
- 验证命令与结果：python3 -m py_compile plot_cross_sections.py 通过；使用空格、逗号和 Tab 三类临时输入完成 Windows Python 端到端测试，成功生成 12 个分类/总览图文件和汇总 CSV，损坏文件被警告并跳过；总览 PNG 视觉检查无裁切、重叠或 MathText 错误。
- 风险、限制和后续建议：WSL 当前未安装 pandas/numpy/matplotlib，运行前需安装依赖；Windows Python 3.13 环境已用 pandas 2.2.3、NumPy 2.1.3 和 Matplotlib 3.9.3 验证。

### 2026-07-30 19:29 — 截断重粒子截面到文献能量上限

- 状态：`已实现`、`已验证`
- 用户目标：修复离子/中性粒子碰撞截面在来源最大能量以上继续使用的问题，重点排除高能电荷交换外推对放电维持诊断的干扰。
- 修改文件：D2/H2-06 至 H2-46 的 23 个重粒子 LXCat/Tabata--Shirai 代理截面文件，以及 tests/test_static_p0.py。
- 修改内容：逐文件读取头部 Energy range 的来源上限，删除全部超上限表格行；在来源上限处补入与求解器线性插值规则一致的端点。自定义反应 9 增加明确的 75 eV--100 keV 推荐范围并删除 100 keV 以上解析外推。新增静态回归测试，要求全部 23 张表能量严格递增、截面非负且最后能量精确等于来源上限。
- 参数变化：反应 11 的 D2+--D2 电荷交换上限 467 keV -> 46.7 keV；反应 10 的 D3+ 生成上限 100 keV -> 17.8 eV；其余多数表由错误的 10 × E_max 恢复为各自来源上限。反应 9 为 1 MeV -> 100 keV。
- 物理或文献依据：Tabata and Shirai, Atomic Data and Nuclear Data Tables 76, 1--25 (2000), DOI 10.1006/adnd.2000.0835；遵守本项目的截面禁止超出来源能区外推规则。
- 未改变的关键行为：未修改截面有效能区内的既有表格点、反应通道、SEE、壁面回收、PID 或达到目标电流后才进入恒流模式的条件。
- 验证命令与结果：两项截面专用回归通过；make test 全部通过，包含边界净电荷/PID、相对论碰撞核心和 10 项静态检查。
- 风险、限制和后续建议：正在运行的旧算例已在启动时载入原截面，必须重启后修复才生效；H/D 同位素代理本身仍有模型不确定性，本次仅消除来源最大能量以上的外推，不擅自缩放有效能区内的截面幅值。

### 2026-07-30 17:24 — 小幅提高离子 SEE 并诊断低电流维持

- 状态：`已实现`、回归测试`已验证`；新参数放电维持效果`待验证`
- 用户目标：解释新算例为何仍不能达到自持目标，并小幅提高离子诱导二次电子发射。
- 最新算例诊断：在约 `0.393 us` 时，电子宏粒子由初始 `339761` 降至 `1533`；体电离累计产生约 `67435` 个电子，反推净非体过程电子损失约 `405663` 个。最近 `0.293--0.393 us` 电子数 `1497 -> 1533`，已接近低密度动态平衡，但尚未形成目标电流放电。
- 启动与控制状态：初始条件仅注入电子、没有同步正离子，早期强非准中性是电子快速排空的重要原因；末步阴极电流约 `0.729 mA`，远低于 `10 mA` 目标，`CC_limited=0`，因此仍按既定逻辑处于 `-80 kV` 恒压启动阶段而未进入恒流 PID。
- 修改文件：`D2/see_materials.dat`、`tests/test_static_p0.py`、`AGENTS.md`。
- 修改内容：只提高离子 SEE 峰值；电子 SEE、快中性粒子 SEE、主电子反射率、壁面吸收与介质边界均保持不变。
- 参数变化：SUS304 `gamma_max: 0.36 -> 0.42`；Duran `gamma_max: 0.17 -> 0.18`。玻璃增幅小于金属，以限制介质表面充电正反馈。
- 物理或文献依据：当前算例末步平均离子 SEE 仅约 `0.102`，低于电子 SEE `0.214` 和总 SEE `0.195`；本轮作为保守灵敏度调整，仍保持 Duran 离子反馈显著低于 SUS304。
- 未改变的关键行为：未改变达到目标电流后才切换恒流的条件、目标误差 PID、反射/SEE 净电荷记账、截面非外推规则、反应通道和 `wall2.wall` 的非电子粒子 100% 吸收。
- 验证命令与结果：`python3 -m unittest tests.test_static_p0` 与 `make test` 全部通过；边界净电荷/PID、相对论碰撞核心和 9 项静态检查通过。
- 生效范围：修改只对下一次启动的算例生效；记录时仍在运行的进程已加载旧参数，不受本次文件修改影响。
- 风险、限制和后续建议：小幅提高离子 SEE 不足以单独解释或弥补当前约 14 倍的目标电流差距；新算例应重点对比早期电子排空、局部玻璃表面电荷与离子入射通量。若仍停留在低电流状态，应优先评估准中性电子/离子启动种子，而不是继续大幅提高玻璃 SEE。

### 2026-07-30 — 按 Physics of Plasmas 风格精简正文与综合图

- 状态：已实现、已验证
- 用户目标：减少空白和无用信息，使图表与论述符合 Physics of Plasmas 常见的紧凑学术表达。
- 修改文件：Paper/main.tex、Paper/sections/03_numerical_model.tex、Paper/sections/05_results_and_discussion.tex。
- 修改内容：将 27 行逐反应清单压缩为 5 类关键碰撞过程；删除仅含内部材料编号和重复“SEE enabled”信息的表格；删除准解析结果的独立占页图，仅在正文中保留其一致性结论与收敛性限制；将 8 张密度/聚变率面板合并为一张 4×2 综合图；取消结果章节前的强制换页。
- 写作与排版依据：采用“现象—图证据—物理解释—限制”的结果论述顺序；相关面板集中编组，图注直接陈述主要物理结论。遵循 AIP 对图表嵌入、相关面板组合及图中文字可读性的作者指南。
- 未改变的关键行为：未修改 D2/H2 算例参数、反应截面、SEE、PID、恒流切换条件、图片数据或色标范围。
- 验证命令与结果：Paper/build.sh 通过，输出缩减为 9 页、3.8 MB；第 7--9 页逐页渲染检查，结果正文连续排布，8 面板综合图完整显示且不再出现两个稀疏浮动页。
- 风险、限制和后续建议：综合图仍使用 PPT 内嵌的物种独立对数色标；定量跨物种比较需由原始场数据重绘统一色标。
### 2026-07-30 — 恢复结果图完整坐标与色标

- 状态：`已实现`、`已验证`
- 用户目标：完整显示 PPT 提取的密度与聚变反应率图。
- 修改文件：`Paper/sections/05_results_and_discussion.tex`。
- 修改内容：移除左右方向的裁切，仅裁去上下无信息白边；恢复 9 张图的完整轴向范围、左右纵轴标注和色标端点。
- 未改变的关键行为：未修改图片数据、色标范围、模拟结果或物理模型。
- 验证命令与结果：`Paper/build.sh` 通过；第 9--11 页重新渲染检查，所有坐标轴、面板和色标完整显示。


### 2026-07-30 — 基于会议海报补充氘等离子体与聚变分布章节

- 状态：`已实现`、`已验证`
- 用户目标：缩小反应网络表格字号，并依据 `Paper/figures/核連講演会ー劉.pptx` 的内容与图片续写论文后续章节。
- 修改文件：`Paper/main.tex`、`Paper/sections/03_numerical_model.tex`、`05_results_and_discussion.tex`、`06_conclusions.tex` 及 9 张新增结果图。
- 修改内容：取消反应表格的全宽等比例放大，恢复 `scriptsize` 学术附表排版；从 PPT 原始媒体中提取 D、D$^+$、D$_2^+$、D$_2$ 数密度与分物种反应率图；新增氘等离子体分布、分物种 D--D 反应率、准解析估算对比和结论。
- 物理解释：带电粒子受静电加速并在中心轴附近聚焦；电荷交换产生的快中性粒子不受电场约束，形成更宽的轴向反应区域。
- 未改变的关键行为：未修改 D2/H2 算例参数、反应通道、截面、SEE、PID、恒流切换条件或已有数值结果。
- 验证命令与结果：`Paper/build.sh` 通过；最终 `Paper/out/main.pdf` 为 12 页、4.0 MB；LaTeX 日志无错误、未定义引用、过宽盒或超大浮动体警告；结果页逐页渲染检查通过。
- 风险、限制和后续建议：新增结果图沿用 PPT 内嵌图的物种独立对数色标；进行定量横向比较时应从原始场数据统一色标并补充统计收敛性分析。

### 2026-07-30 — 学术化 Paper 正文与 PIC-MCC 流程图

- 状态：`已实现`、`已验证`
- 用户目标：删除论文中“读取文件/输入路径”等工程化描述，并提高正文、图题和图片的学术表达质量。
- 修改文件：`Paper/main.tex`、`Paper/sections/01_introduction.tex`、`02_experimental_setup.tex`、`03_numerical_model.tex`、`04_validation.tex`、`Paper/figures/pic_mcc_cycle.tex`、`pic_mcc_cycle.pdf`。
- 修改内容：以反应网络、截面适用范围、材料边界和谱线合成方法替换文件名/路径/读写操作描述；改写主要图题；统一恒流验证为 voltage--pressure characteristic；删除“由硕士论文改成期刊草稿”等非论文式致谢文字。
- 图片变化：用可复现 TikZ 矢量图替换细长 PNG 程序流程图；新图采用横向双栏布局、色盲友好配色和物理算子，显式标出 PIC、碰撞/表面过程及恒流反馈。
- 物理或文献依据：未改变反应阈值、截面数据、SEE 参数、恒流切换条件或计算结果；明确写入截面不超过来源能量范围外推。
- 验证命令与结果：`Paper/build.sh` 完整通过并生成 8 页、2.9 MB 的 `Paper/out/main.pdf`；前 8 页全部渲染检查，新流程图和正文无裁切、重叠或不可读文字。
- 风险、限制和后续建议：三张验证结果图缺少独立原始绘图数据/脚本，暂未重构其数值曲线；后续获得数据后应统一字体、线宽、图例和误差表示。

### 2026-07-30 — 配置 Paper 的 AIP/Overleaf 论文模板

- 状态：`已实现`、`已验证`
- 用户目标：下载指定 Overleaf AIP 期刊模板，放入 `Paper/` 并复用其中已有章节、参考文献和图片资源。
- 修改文件：`Paper/main.tex`、`Paper/README.md`、`Paper/sections/03_numerical_model.tex`。
- 修改内容：保留官方示例 `Paper/aipsamp.tex`；以 `main.tex` 为工作主文件，接入 `sections/`、`figures/` 和 `references.bib`；补充 Overleaf 与本地构建说明；修复反应表格在当前 LaTeX 下的列格式兼容问题。
- 参数变化：工作主文件 `revtex4-1 -> revtex4-2`，以兼容当前 TeX Live/Overleaf，同时保留 AIP、AMS 和 `reprint` 选项；官方示例不改动。
- 物理或文献依据：指定 Overleaf 模板页面及其公开源文件说明；本次只涉及论文排版，不涉及物理模型参数。
- 未改变的关键行为：未修改 D2/H2 算例、恒流切换、PID、电流记账、反应截面或玻璃壁模型。
- 验证命令与结果：`Paper/compile.sh`（pdfLaTeX + BibTeX）和 `Paper/build.sh`（LuaLaTeX + BibTeX）均完整通过，生成 `Paper/out/main.pdf`（2.9 MB）。
- 风险、限制和后续建议：投稿前按目标 AIP 期刊要求确认 `reprint`/`preprint` 选项及通讯作者信息。

### 2026-07-30 — 按末段粒子收支定向提高金属壁反馈

- 状态：`已实现`、`已验证`
- 用户目标：解决电流和带电粒子持续衰减，判断并缓解过于保守的 SEE/主电子反射设置。
- 最新 100 万步（10 us）算例证据：末 1 us 电子数 `2850 -> 2203`（`-22.7%`）；八个电离通道合计生成约 `8387` 个电子宏粒子，反推净边界损失约 `9034` 个，维持阈值尚差约 `7%`。
- 同一末段平均：`see_yield_electron=0.2480`、`see_yield_ion=0.0888`、`I_tot_cathode=0.1436 mA`；电压已在 `-60 kV` 下限，控制器无法再靠增大电压绝对值补偿。
- 末步撞击谱显示带电粒子主要到达金属电极；玻璃壁主要接收 D/D2 中性粒子，因此不继续提高 Duran SEE。
- 阳极高能电子主反射：`20% -> 30%`（吸收率 `0.80 -> 0.70`）；阴极：`35% -> 40%`（吸收率 `0.65 -> 0.60`）；玻璃仍为最多 `10%`。
- SUS304 SEE：电子 `delta_max 1.45 -> 1.50`，离子 `gamma_max 0.30 -> 0.36`，快中性粒子 `gamma_max 0.035 -> 0.045`。
- Duran 保持 `1.30/0.17/0.022`；`wall2.wall` 仍保持所有非电子粒子 100% 吸收，以遵守既定玻璃壁模型并限制介质充电正反馈。
- 未改变恒压达到目标后再转恒流的流程、目标误差 PID、反应截面和反应通道。
- 文献依据：NIST 的不锈钢离子诱导发射研究确认产额随入射能量和表面氧覆盖变化：<https://www.nist.gov/publications/low-energy-ion-induced-electron-and-ion-emission-stainless-steel-effect-oxygen-coverage>；NBS/NIST 30 keV 铁实验与蒙特卡洛结果支持金属高能电子存在显著背散射且随入射角上升：<https://nvlpubs.nist.gov/nistpubs/Legacy/SP/nbsspecialpublication460.pdf>。
- 验证：`make test` 全部通过；边界净电荷/PID、相对论碰撞核心和 9 项静态检查通过。新参数能否维持自持放电仍待新算例验证。

### 2026-07-29 — 小幅提高 SEE 以改善自持放电

- 状态：`已实现`、`已验证`
- 最新算例末 200 个采样的累计平均值：`see_yield_total=0.0954`、`see_yield_electron=0.2328`、`see_yield_ion=0.0829`、`see_yield_neutral=0.0030`。

- 电子 SEE：SUS304 `1.35 -> 1.45`，Duran `1.20 -> 1.30`；目标是让电子诱导平均产额接近 0.25。
- 离子 SEE：SUS304 `0.25 -> 0.30`，Duran `0.15 -> 0.17`。
- 快中性粒子 SEE：SUS304 `0.030 -> 0.035`，Duran `0.020 -> 0.022`。
- 保持阳极/阴极/玻璃的主电子最大反射率 `20%/35%/10%`，避免与提高后的 SEE 叠加成强反馈。
- 未改变 PID、恒流切换、反应截面或壁面吸收模型。
- 增加静态回归检查锁定本轮 SEE 参数。
- 验证：`make test` 全部通过；边界电荷/PID、相对论碰撞核心和 9 项静态检查通过。新 SEE 对自持性的效果仍需新算例确认。

### 2026-07-29 — 优化阳极、阴极与玻璃壁反馈

- 状态：`已实现`、`已验证`
- 用户目标：降低其他边界引起电子倍增或异常鞘层的风险，同时保留 SEE 对放电维持的贡献。
- 修改文件：`D2/input`、`D2/anode.wall`、`D2/cathode.wall`、`tests/test_static_p0.py`。
- 阳极高能电子吸收率：`0.10 -> 0.80`，主电子最大反射率：`90% -> 20%`。
- 阴极高能电子吸收率：`0.20 -> 0.65`，主电子最大反射率：`80% -> 35%`。
- 玻璃壁反应文件：`wall.wall -> wall2.wall`；非电子粒子恢复为 100% 吸收，高能主电子最多 10% 反射。
- 保持 SUS304/Duran SEE 参数、离子/中性粒子电极回收率、PID 和恒流模式切换逻辑不变。
- 增加静态回归检查，锁定三个边界的高能电子吸收率和玻璃壁反应文件映射。
- 物理依据：避免把 true SEE 与过高的主电子背散射概率叠加为非物理强反馈；阴极保留比阳极更高的反射以兼顾放电维持。
- 验证：`make test` 全部通过；边界电荷/PID、相对论碰撞核心和 9 项静态检查通过。

### 2026-07-29 — 建立连续工作模式

- 状态：`已实现`、`已验证`
- 新建本文件，集中记录工作规则、当前物理基线、不可破坏约束、文献和验证方法。
- 将玻璃壁面从过度保守的 SEE 隔离参数恢复为中等降额参数；保留放电维持能力，同时限制玻璃壁离子 SEE 和主电子反射。
- 玻璃壁高能电子吸收率由 25% 提高为 90%，SEE 映射重新开启。
- 未修改 PID、恒流模式切换条件或反应截面。
- 验证：`make test` 全部通过；边界电荷/PID、相对论碰撞核心和 8 项静态检查通过。

## 7. 后续日志模板

复制以下内容到“修改日志”顶部：

```markdown
### YYYY-MM-DD HH:MM — 简短任务名称

- 状态：`已实现` / `已验证` / `待验证` / `已回退`
- 用户目标：
- 修改文件：
- 修改内容：
- 参数变化：`旧值 -> 新值`
- 物理或文献依据：
- 未改变的关键行为：
- 验证命令与结果：
- 风险、限制和后续建议：
```
